---
import Container from "../shared/Container.astro";
import Title from "../shared/Title.astro";
import Button from "../shared/Button.astro";
import Label from "../elements/Label.astro";
import Paragraph from "../shared/Paragraph.astro";
if (Astro.request.method === "POST") {
  try {
    const data = await Astro.request.formData();
    const name = data.get("username");
    const email = data.get("email");
    const password = data.get("password");
    // Do something with the data
  } catch (error) {
    if (error instanceof Error) {
      console.error(error.message);
    }
  }
}
---

<section id="reserve" class="pt-8">
  <Container className="space-y-10 md:sapce-y-12 p-16">
    <div class="text-center max-w-3xl mx-auto space-y-4">
      <Title>Datos de contacto</Title>
    </div>
    <form method="POST" class="flex flex-col gap-5  mx-auto w-full items-center">
      <Label >
        <Paragraph>Nombre:</Paragraph>
        <input
          class="p-2 rounded-3xl border border-box-border bg-box-bg shadow-lg shadow-box-shadow relative overflow-hidden"
          type="text"
          name="username"
          placeholder="Carolina Durante"
          required
        />
      </Label>
      <Label>
        <Paragraph>Email:</Paragraph>
        <input
          class="p-2 rounded-3xl border border-box-border bg-box-bg shadow-lg shadow-box-shadow relative overflow-hidden"
          type="email"
          name="email"
          placeholder="carolinadurante@gmail.com"
          required
        />
      </Label>
      <Label>
        <Paragraph>Número de teléfono:</Paragraph>
        <input
          class="p-2 rounded-3xl border border-box-border bg-box-bg shadow-lg shadow-box-shadow relative overflow-hidden"
          type="text"
          name="phone-number"
          placeholder="646 658 814"
          required
          minlength="6"
        />
      </Label>
      <Label>
      <Paragraph>Acompañantes:</Paragraph>
        <input
          class="p-2 rounded-3xl border border-box-border bg-box-bg shadow-lg shadow-box-shadow relative overflow-hidden"
          type="number"
          step="1"
          name="phone-number"
          required
          minlength="6"
          value="0"
        />
      </Label>
      <Label>
        <Paragraph>Precio total:</Paragraph>
        <Paragraph><span class="text-3xl">12€</span></Paragraph>
      </Label>
      <Button className="w-full sm:w-1/2" variant="primary">Enviar</Button>
    </form>
    <Paragraph className="text-center">
      Esta información la utilizaremos para contactar contigo e indicarte los pasos para el pago. Al completar el formulario te llegará un correo con la entrada.
      Esta entrada funcionará hasta completar el pago.
    </Paragraph>
  </Container>
</section>
